services:
  - type: web
    name: smartmeter-forecast
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 300 --workers 1 --threads 2 --log-level info --bind 0.0.0.0:$PORT
    plan: free
    healthCheckPath: /health
